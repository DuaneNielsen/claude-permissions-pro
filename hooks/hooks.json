{
  "description": "Smart permission hook — parses chained bash commands, auto-approves safe patterns, optionally uses LLM judge for unknowns",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/pretooluse.py",
            "timeout": 10000
          }
        ]
      }
    ]
  }
}
